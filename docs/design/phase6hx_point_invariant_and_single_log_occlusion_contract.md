# Phase 6HX canonical Point invariant and single-log occlusion contract

Phase 6HW remains frozen at `16f1e6c` as
`safe_stop_pre_kit_harness_failure`. Its artifact root, contract, result, and
classification are not reused. Phase 6HX changes only the parent production
invariant boundary before rebuilding the same fixed diagnostic scene in a new
root.

## Canonical production Point-policy source set

The former nonexistent `point_emitter.py` is not replaced by another guessed
single filename. Production `campfire.app` registration, public imports, and
`CampfireAppExtension` composition show that Point policy spans these ordered
roles:

1. `config/extension.toml`: extension registration, dependencies, settings.
2. `app/__init__.py`: public production import/export surface.
3. `app/extension.py`: production composition and scene lifecycle consumer.
4. `resident_native_backend.py`: authoritative native payload/revision source.
5. `resident_snapshot.py`: immutable published field/revision schema.
6. `resident_snapshot_adapter.py`: ordered USD publication/revision checks.
7. `resident_application_session.py`: owner-thread publication transaction.
8. `resident_point_sidecar.py`: Point shape, dtype, identity, order, revision.
9. `resident_point_scene.py`: default-off settings, layout, source, emitter.
10. `resident_point_application_owner.py`: application owner/layout revision.
11. `resident_point_commands.py`: owner-thread command sequence/order.
12. `resident_point_continuity.py`: contiguous group/alignment evidence.
13. `resident_stage_recovery.py`: replacement/revision/recovery ownership.

The versioned manifest records repository-relative paths, roles, deterministic
order, and SHA-256. The shared producer and parent validator reject missing or
non-regular files, the legacy path, duplicate or reordered entries, hash
mismatch, root escape, reparse points, schema/digest mismatch, and different
producer/consumer sets. The 7,287-byte bounded report remains well below its
128 KiB maximum.

The no-Kit preflight must qualify the real producer-to-file-to-bounded-reader-
to-parent path, Phase 6HU atomic fixture, generated-stage fixture, and the
formal parent command in `--prelaunch-only` mode. Its durable
`production_invariant_hash_complete` marker and Kit launch count zero are hard
gates.

## Runtime scene and immutable comparison

The scene, mask, ROI, thresholds, camera, source, capture window, and resource
contract are copied into a new Phase 6HX contract without relaxing Phase 6HW:

- one X-axis diagnostic log, length 3.6 m, radius 0.16 m, center Z 0.90 m;
- the Phase 6HS 26-vertex / 36-face / 120-index closed outward cross-section;
- Sphere source center Z 0.48 m, radius 0.20 m, surface gap 0.06 m;
- end-on camera from `(5.5, 0, 0.9)`, world-Z image-up, 1280x720;
- active-block liveness at frames 60/120/180/240, minimum 128 each;
- Flow-only captures at frames 120 through 240, stride 10;
- the same Flow-color mask, occupancy threshold, six ROIs, numeric hard gates,
  and human gate as Phase 6HW;
- fresh OFF then fresh ON, one process each, retry and replacement zero;
- generated stages differ only in `physicsCollisionEnabled`.

The operation uses no NanoVDB readback, volume conversion, sampling, temporary
NVDB, Point publication, dynamic transform, PhysX sharing, or performance
work. Kit remains limited to 16 GiB and the unique tree to 17 GiB, with the
Phase 6HU atomic report, accepted lifecycle classification, exact identity
cleanup, and residual-zero gates.

Automated temporal occupancy and human review are both required. A pass can
qualify only the visual signature of this axis-aligned diagnostic log and
directly-below Sphere source. Production placement, arbitrary pose, dynamic
tracking, Point coexistence, PhysX sharing, 4/20-log performance, P3/P4,
production integration, and default-on remain unqualified.

## Frozen result

The canonical Point-policy source-set boundary qualified before runtime. The
actual producer-to-consumer fixture passed all 13 cases, the inherited atomic
report fixture passed 15/15, and the generated-stage fixture passed 11/11. A
formal-command prelaunch smoke reached
`production_invariant_hash_complete` with zero Kit launches. The manifest is
`3D0BD6CDBDA2FA0FEE38FA390EF65C1BD763EEEA80024F0ED6F96C688F51645D`;
its ordered-entry digest is
`283C59CB4C559D0437BD0CBA7F66FCC02712365C81D2AFEDCAF0E03BFCB4740A`.

The fresh formal root then stopped on its first and only OFF launch. Kit's
actual `--exec` environment could load the wrapper path but could not import
`phase6hx_probe_source`; stderr records `ModuleNotFoundError` before any probe
operation or lifecycle marker. The canonical operation report is therefore
absent. The guard later reached its absolute bound, collected bounded shutdown
evidence, and removed the exact attempt tree. ON was not launched. No active
blocks, capture, occupancy metric, human visual result, or video exists.

This is `safe_stop_kit_import_harness_failure`, not Flow, CollisionProxy,
resource, rendering, or occlusion evidence. Kit/tree peaks were
9,129,500,672/9,319,284,736 bytes, below the frozen 16/17 GiB limits; the
observed physical/commit minima stayed above 8 GiB. Fatal, device-loss, TDR,
dump, automatic upload, and final residual counts were zero. Phase 6HW remains
unchanged and is not reclassified.

A future separately approved phase may change only the deterministic Kit
`--exec` import boundary, qualify an app-ready exact-wrapper smoke, and then
use another empty root. It must not reuse this runtime attempt. Production,
defaults, Point policy, wood authority, V3, public scene, and latest demo were
not changed.
