# Phase 6HT static production-hierarchy Flow occlusion contract

Phase 6HS (`fbbac6f`) remains an immutable qualification of one default-off
26/36/120 closed outward Mesh CollisionProxy in the production Log_00
hierarchy. Phase 6HT does not modify or reuse its runtime population. It uses
that exact proxy authoring and transform as a safe baseline, then compares two
fresh processes whose only authored Flow difference is
`FlowSimulate.physicsCollisionEnabled`.

The fixed order is Collision ON followed by Collision OFF, one process each,
with no retry or replacement. The stage, source at `(0,-0.42,0)` with radius
0.10 m, Log_00 transform, camera, 960x540 render settings, 240 playing-timeline
updates, and active-block observations at frames 60/120/180/240 are identical.
The Flow and capture authoring reuses the known-good Phase 6DS/6EO helpers;
the proxy uses the exact Phase 6HS derived source. No analytic Cylinder,
NanoVDB readback, volume conversion, temporary NVDB, sampling, Point policy,
dynamic transform, PhysX sharing, production integration, or performance
extension is in scope.

The machine contract and frozen thresholds are in
`scripts/phase6ht_static_flow_occlusion_contract.json`, with SHA-256
`AC5571BBFF59456285B745226137DE5055159A3628F46083BA47DC32C43E73A2`.
Each condition must pass the unchanged Phase 6HS canonical operation report,
resource guard, role validation, lifecycle classification, and exact cleanup.
All four active-block samples must be at least 128. Fixed baseline/final image
ROIs require an OFF sensitivity signal, ON direct-path suppression, continued
ON Flow outside the direct path, and a material ON/OFF image difference. A
human visual review remains a hard gate: ambiguous evidence is a safe stop.

If qualified, the result is limited to one static proxy in the production wood
hierarchy under these fixed settings while the timeline is playing. It does
not qualify production defaults, dynamic tracking, PhysX mesh sharing, Point
Emitter coexistence, or four-/twenty-log performance.
