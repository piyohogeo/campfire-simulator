# Phase 6GA S93 / S100 comparison harness safe stop

## Frozen scope and contract

Phase 6GA is a fresh, production-neutral restart boundary after Phase 6FZ. Its pre-runtime contract is `campfire.phase6ga.supply-comparison-contract.v1`, SHA-256 `F0733DFA6A68207289892FB12EBE4191AC5D869EE0CF5D82D00CABD186B5AD42`. The formal population was fixed as three balanced sequences of S93 collision ON, S100 collision ON, and an otherwise identical S100 collision OFF control. Each condition retained the corrected four-log geometry, the frozen Point payloads and source sums, three public readbacks, signed authored-Mesh boundary/deep regions, directional control-volume transport, 16 GiB Kit and 17 GiB unique-tree stops, release-after-close, progress-aware CDB, durable pre-close commit, and Phase 6FU/6FW exact identity cleanup. Phase 6FO and Phase 6FZ evidence were not reused or reclassified.

The scalar contract does not require exact zero. It compares baseline-subtracted temperature, smoke, and fuel spatial integrals and opposite-side directional transport; collision-ON deep velocity retains the `1e-4 m/s` engineering maximum. S100 must preserve at least a 1.07 weighted-fuel ratio over S93 without exceeding the predeclared 1.25 deep-scalar, opposite-transport, or floored deep-velocity ratios. The collision-OFF control requires at least `0.1 m/s` deep velocity and S100/OFF deep-velocity suppression of 0.01 or better. Visible direct penetration remains a fail-closed review after numeric qualification.

## Confirmed safe-stop boundary

The new root `artifacts/phase6ga-supply-comparison-1` passed the inherited app-ready import and CDB fixtures, and its offline corrected-four geometry audit passed. The evidence-only channel preflight then stopped during PowerShell parameter binding, before Kit creation. The contract supplied runtime geometry token `corrected`, while the directly reused case runner accepts only `legacy_phase6ep` or `phase6er_corrected`. Its stderr records `ParameterArgumentValidationError` for `GeometryVariant`; the guard observed Kit peak 0 bytes and one resource sample. Therefore no stage, Flow field, readback, S93/S100/OFF formal process, scalar/flux result, or visual capture exists in this Phase.

This is a harness operation failure, not a Flow, memory-ceiling, collision, readback, or native lifecycle result. It is nonreplaceable under the frozen contract, so channel attempt 01 was not retried and all nine formal slots remained unstarted. The pre-close committer correctly reported `parent_stopped_before_measurement_commit`; that missing artifact is a consequence of process entry failure and is not treated as a committed sample.

The Phase 6FU guard verified dual-source absence and Phase 6FW classified all three observed identities as ended. Peaks were 495,616 bytes for the runner, 16,195,584 bytes for the isolated diagnostic helper, and 19,505,152 bytes for the unique tree; Kit was never observed. No CDB attach, fatal, dump, upload, device loss, TDR, or residual process occurred. Production app SHA-256 remained `94162F82AF95D5ABB3798FCB5CA71F7821B7813FD8623D1387BC723288ADF02A`, and the latest-demo manifest was unchanged.

Release build, Phase 0 RTX, and Phase 3 passed. Phase 3 retained zero dry/wet mass-balance error, wood-owned Flow input, active blocks final/peak 238/320, and peak fuel 1.0. Focused Phase 6F contracts passed 212/212; the standard suite passed all 8 processes and 78/78 tests in 327.4 seconds; devlog validation reported 473 references, 283 unique IDs, 235 JSON, 177 SVG, and 2 ZIP assets. Final Kit/CDB/nvidia-smi residual count was zero.

## Continuation boundary

Phase 6GA remains a frozen safe stop and must not be converted into a comparison result. The next authorized restart requires an independent contract/root that maps the already-audited `corrected` geometry concept to the case runner's existing `phase6er_corrected` runtime token before any Kit process is launched, plus a parameter-binding fixture that exercises the exact command line. Physical geometry, Point policies, source sums, Flow settings, numeric thresholds, 16/17 GiB limits, and lifecycle/cleanup policy do not need to change. Until that harness-only correction is independently frozen and approved, S93/S100 comparison, comparison video, Point Emitter–CollisionProxy adoption, defaults, production, and P4 remain blocked.
