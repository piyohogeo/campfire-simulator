# Phase 6GN exact-wrapper supply comparison

## Frozen history and scope

Phase 6GK remains the qualified public-channel preflight. Phase 6GL remains a
channel-operation safe stop, and Phase 6GM remains a nonreplaceable exact-wrapper
import safe stop with 0/9 accepted formal processes. Its first S93 launch, the
900-second Kit residual, partial CDB evidence, and exact cleanup result are not
reclassified or reused.

Phase 6GN changes only the diagnostic wrapper import boundary. It does not
change production code, the corrected-four geometry, Point payload, Flow export
state, CollisionProxy, physical thresholds, resource ceilings, lifecycle order,
or comparison population.

## Type-aware import contract

The generic exact loader continues to treat its `required_entrypoints` as
callable functions. Phase 6GN therefore passes only
`_qualified_spatial_boundary` through that interface. The `shared` attribute is
validated separately as `types.ModuleType`, with an exact resolved path to
`probe_phase6gc_shared_supply_comparison.py` in the current Kit process.

The shared module must expose callable `_run`, `_build_stage`,
`_append_resource_marker`, `_p3_world_rois`, `_bounded_object_metadata`,
`_save_and_sample`, `process_memory_snapshot`, and `_type_name`. Its `Usd` and
`point_core` attributes must be modules. Missing attributes, a wrong module
type or path, and noncallable function attributes fail closed. The module
object itself is never required to satisfy `callable()`.

The wrapper must patch the exact `shared._build_stage` target with
`_build_stage_with_qualified_exports`, retain a distinct callable original,
and validate immutable export descriptor digest
`53CDE38FD5B1A5F48AB2E7B896F6EF391DDA4D5F6621B21FDB1B435F34BDA8CE`.
The import audit records PID, resolved paths, attribute types, loader `sys.path`,
and wiring identities in bounded JSON.

## Pre-runtime qualification

Before any formal slot, a real Kit app-ready `--exec` smoke imports the exact
Phase 6GN wrapper with the formal module path and patch wiring. It performs no
stage authoring, timeline operation, Flow operation, readback, or formal slot
consumption. Durable markers are `import_started`, `import_complete`,
`wrapper_wiring_complete`, and `smoke_complete`. Wrong path, the legacy
shared-as-callable declaration, and missing attributes must be rejected; an
intentional nonzero exit verifies child-to-parent exit-code propagation. Every
case requires bounded exit and zero residual Kit process.

The existing payload-native source 16-case fixture, corrected geometry binding,
Phase 6GK schema fixture, canonical bounded-artifact fixture, saved/reloaded USD
export-state fixture, generic app-ready import smoke, and progress-aware CDB
fixture remain mandatory.

## Formal population and safety

Only after all preflight evidence passes may a new empty artifact root start
the balanced S93/S100/OFF population from process 1. The nine accepted process
contract, at most two startup-only replacements, exact three readbacks at
frames 180/360/540, spatial definitions, transport proxy, hard gates, 16/17 GiB
ceilings, release-after-close order, durable pre-close commit, stack-first CDB,
identity cleanup, and 180-second stage-close timeout are inherited unchanged.
Import and every non-startup failure are nonreplaceable.

Comparison video remains forbidden until numeric 9/9 qualification. Even then,
this Phase may report only an S93 or S100 candidate; production integration and
default changes remain out of scope.

## Runtime result: safe stop

All inherited pre-Kit gates and the exact-wrapper app-ready smoke passed. The
standalone and formal-root exact smokes each completed 5/5, including the exact
wrapper import, module/path/type contract, patch wiring, immutable descriptor,
three negative cases, exit code 29 propagation, and zero residual. The formal
attempt01 therefore started with the import defect fixed.

The first fresh S93 process reached representative frame 180 with 1,329 active
blocks, an exact 1,344/1,440 payload, and the expected payload-native source
sums. Public readback returned seven handles. The last durable marker is
`phase6gl_readback_after`; no spatial result or pre-close measurement artifact
was committed after it. Kit subsequently exited with Windows access violation
`0xC0000005`. The crash reporter recorded one fatal and one bounded dump, with
no automatic upload. Its low-confidence backtrace is in
`omni.kit.app.plugin.dll!carbOnPluginQuickShutdown`, then `ucrtbase!recalloc`;
this does not establish the originating owner or operation.

The failure occurred before timeline stop or stage close, so it is not a
release-after-close result and the stage-close CDB path was not invoked. Kit and
tree peaks were 15,326,326,784 and 15,478,104,064 bytes, inside the 16/17 GiB
limits. Exact identity cleanup confirmed all 37 observed attempt identities
absent and final residual zero.

This is a nonreplaceable native operation/lifecycle safe stop: launched 1,
accepted 0/9, replacements 0. No later condition, paired metric, directional
flux, deep/boundary result, comparison video, candidate selection, or production
change was started. A future Phase requires separate approval and a new root to
isolate the frame-180 post-readback access violation; Phase 6GN is not retried.
