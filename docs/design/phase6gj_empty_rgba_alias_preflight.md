# Phase 6GJ empty RGBA alias preflight

Phase 6GI remains a frozen safe stop. Its one runtime sample, artifact root,
classification, and all thresholds are history and are not reused or
reclassified by this Phase. Phase 6GJ freezes contract SHA-256
`67CC3F86DF177815F04D05097E49AE4D2D5FA4D88343E80B5980BDD0F167AC69`
before runtime and permits one fresh corrected-four S93 public-channel
preflight in a new artifact root. Formal S93/S100/OFF comparison, spatial
aggregation, flux, deep velocity, video, production integration, defaults,
Point placement, V3, P4, and dynamic geometry remain outside authorization.

The candidate schema remains
`flow110.0.0-kit110.2-public-readback-rgba7-v1` for Flow 110.0.0 and Kit
110.2. Nonempty handles 0--5 must be the same Python object as their local
alias, must share NumPy memory, must match the exact grid class, value type,
shape, logical byte count, and positive pointer requirements, and must leave no
weak-reference residual after ordered release. Disabled RGBA handle 6 must be
the same Python object, have zero elements and zero logical bytes, create no
material copy, and leave no weak-reference residual. `numpy.shares_memory` for
that zero-length array is telemetry only: an empty array has no element whose
overlap can establish ownership, so a `false` result is neither failure nor
evidence of a copy.

The pre-Kit policy fixture covers nonempty shared/nonshared aliases, valid
empty aliases, each invalid empty property, exact handle count/order/type, and
unknown, duplicate, missing, and future schemas. It also serializes and reloads
the same bounded observation shape used by the runtime report. The fixture
must pass before Kit starts. The runtime remains one public readback at frame
180 with divergence enabled and RGBA/RGB disabled. Handles are processed and
released sequentially; no `np.asarray`, body JSON/NPZ/OpenVDB, spatial metric,
flux, deep velocity, image, or video is allowed.

Normal 16/17 GiB Kit/tree limits, 512 MiB runner/diagnostic limits, 8 GiB
physical/commit floors, release-after-close, progress-aware stack-first CDB,
durable pre-close commit, exact attempt-tree cleanup, PID reuse policy, and the
startup-only limited replacement rule are unchanged. Every operation, schema,
resource, pointer, lifecycle, artifact, identity, or cleanup failure is
nonreplaceable and stops the Phase. Qualification requires exact seven-handle
schema and alias gates, zero weak-reference residual, complete stage close,
normal OS exit, safe resources, exact cleanup, and residual process count zero.

## Runtime result

The offline alias/schema fixture passed 22/22, including the serialized raw
artifact round trip; the startup replacement fixture passed 12/12. The one
fresh process was representative from frame 1 and reached 1,329 active blocks
at frame 180. Its one public readback returned the exact seven-handle schema.
Handles 0--5 were nonempty same-object aliases with shared memory and the
required metadata. Disabled RGBA was the same Python object with zero elements,
zero bytes, no material copy, and a false non-required sharing predicate. All
seven weak references were dead after sequential release. The state-aware raw
schema and alias validation therefore passed, with zero `np.asarray`, copy, or
field-body write.

Phase 6GJ nevertheless safe-stopped as a nonreplaceable artifact failure. The
shared `run_phase6gd_channel_metadata_probe.ps1` post-run check requires a
property named `full_field_json_or_npz_written` on the bounded observation,
while the new probe durably reports the equivalent bounded-scope fact as
`field_body_json_npz_or_openvdb_written`. Strict PowerShell property access
raised after Kit had exited normally. The inner Kit run had functional pass,
normal OS exit, stage close in 3.167604 seconds, `shutdown_complete`, and exact
cleanup; the shared child wrapper exited 1, so the Phase 6GJ parent correctly
refused formal acceptance. This latent artifact-interface mismatch is not an
alias, schema, Flow, resource, or native lifecycle failure, but it prevents a
preflight-qualified status. No replacement or retry was performed.

Kit/tree peaks were 15,293,259,776 / 15,457,742,848 bytes, leaving
1,886,609,408 / 2,795,868,160 bytes below 16/17 GiB. Runner/diagnostic peaks
were 119,894,016 / 16,936,960 bytes; physical and commit headroom minima stayed
above 82/102 GB. CDB, fatal, dump, upload, and residual process counts were
zero. The candidate schema remains not formally preflight-qualified. A future
explicitly approved Phase must freeze the bounded-output property contract and
use another empty root; Phase 6GJ may not be rerun or reclassified.

Final regression passed the Release build, Phase 0 RTX, Phase 3 with zero
dry/wet mass-balance error and wood-owned Flow input (active blocks final/peak
303/332, peak fuel 1.0), focused Phase 6F 212/212, focused Phase 6G 53/53, and
the standard eight-process 78/78 suite in 312.9 seconds. Devlog validation
passed 486 references, 290 unique IDs, 242 JSON, 177 SVG, and two ZIP files.
Production and latest-demo hashes were unchanged and final Kit/CDB/GPU-helper
residual count was zero. No new video was created.
