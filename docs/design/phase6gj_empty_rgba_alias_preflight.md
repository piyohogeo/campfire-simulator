# Phase 6GJ empty RGBA alias preflight

Phase 6GI remains a frozen safe stop. Its one runtime sample, artifact root,
classification, and all thresholds are history and are not reused or
reclassified by this Phase. Phase 6GJ freezes contract SHA-256
`67CC3F86DF177815F04D05097E49AE4D2D5FA4D88343E80B5980BDD0F167AC69`
before runtime and permits one fresh corrected-four S93 public-channel
preflight in a new artifact root. Formal S93/S100/OFF comparison, spatial
aggregation, flux, deep velocity, video, production integration, defaults,
Point placement, V3, P4, and dynamic geometry remain outside authorization.

The candidate schema remains
`flow110.0.0-kit110.2-public-readback-rgba7-v1` for Flow 110.0.0 and Kit
110.2. Nonempty handles 0--5 must be the same Python object as their local
alias, must share NumPy memory, must match the exact grid class, value type,
shape, logical byte count, and positive pointer requirements, and must leave no
weak-reference residual after ordered release. Disabled RGBA handle 6 must be
the same Python object, have zero elements and zero logical bytes, create no
material copy, and leave no weak-reference residual. `numpy.shares_memory` for
that zero-length array is telemetry only: an empty array has no element whose
overlap can establish ownership, so a `false` result is neither failure nor
evidence of a copy.

The pre-Kit policy fixture covers nonempty shared/nonshared aliases, valid
empty aliases, each invalid empty property, exact handle count/order/type, and
unknown, duplicate, missing, and future schemas. It also serializes and reloads
the same bounded observation shape used by the runtime report. The fixture
must pass before Kit starts. The runtime remains one public readback at frame
180 with divergence enabled and RGBA/RGB disabled. Handles are processed and
released sequentially; no `np.asarray`, body JSON/NPZ/OpenVDB, spatial metric,
flux, deep velocity, image, or video is allowed.

Normal 16/17 GiB Kit/tree limits, 512 MiB runner/diagnostic limits, 8 GiB
physical/commit floors, release-after-close, progress-aware stack-first CDB,
durable pre-close commit, exact attempt-tree cleanup, PID reuse policy, and the
startup-only limited replacement rule are unchanged. Every operation, schema,
resource, pointer, lifecycle, artifact, identity, or cleanup failure is
nonreplaceable and stops the Phase. Qualification requires exact seven-handle
schema and alias gates, zero weak-reference residual, complete stage close,
normal OS exit, safe resources, exact cleanup, and residual process count zero.

Runtime result is intentionally not recorded in this pre-runtime contract
section.
